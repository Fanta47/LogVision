input {
  file {
    path => "/usr/share/logstash/uploads/raw/*"
    start_position => "beginning"
    sincedb_path => "/usr/share/logstash/data/sincedb"
  }
}

filter {
}

output {
  elasticsearch {
    hosts => ["http://elk-es01-1:9200"]
    index => "logstash-test-data"
  }
  stdout { codec => rubydebug }
}
